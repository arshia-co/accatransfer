name: Deploy ACCA Transfer

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install
        run: npm ci

      - name: Build
        env:
          VITE_SUPABASE_URL: https://qysluhfrjpcguhneqsuz.supabase.co
          VITE_SUPABASE_ANON_KEY: sb_publishable_uhclsIDMqnKxbUzkbgcYsg_a70CzU29
          # Cloudflare Turnstile site key (public). Set repo Variable
          # VITE_TURNSTILE_SITE_KEY in GitHub → Settings → Secrets and variables →
          # Actions → Variables to enable the anti-bot gate. Empty = gate disabled.
          VITE_TURNSTILE_SITE_KEY: ${{ vars.VITE_TURNSTILE_SITE_KEY }}
        run: npm run build

      - name: Add direct SPA route entries
        run: |
          for route in smart-apply ai-transfer account smart-apply/account ai-transfer/account; do
            mkdir -p "dist/$route"
            cp dist/index.html "dist/$route/index.html"
          done

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - name: Deploy
        uses: actions/deploy-pages@v4
        id: deployment
